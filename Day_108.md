# Building Projects

- test(integration): verify task update logic and timestamps
  - Implemented full integration suite for service-update-task
  - Used Vitest fake timers to verify accurate 'updatedAt' field injection
  - Verified state isolation by ensuring non-targeted tasks remain unchanged
  - Handled edge cases for non-existent IDs returning null

- fix(integration): solve the problem when i run all tasks at one

- test(integration): document known race conditions and timezone issues
  - Commented out failing integration tests for service-list-tasks and service-update-task
  - Added BUG notes regarding parallel file system access conflicts
  - Documented timezone dependency for timestamp verification
  - Planned refactor: Implement sequential execution and TZ normalization

- test(integration): verify task status transition logic
  - Verified marking tasks as done, in-progress, and not-done
  - Ensured file system updates persist correctly after marking
  - Validated that status changes do not affect unrelated tasks
  - Handled null returns for non-existent IDs

- test(e2e): verify successful task creation flow
  - Validates that 'add' command triggers the full lifecycle (Router -> Controller -> Service -> View)
  - Confirms terminal output matches expected success format and colors

- test(e2e): verify validation error reporting for add command
  - Confirms that missing descriptions are caught by middleware
  - Validates that error messages are correctly piped to console.error

- test(e2e): verify task listing and view formatting
  - Validates that the 'list' command correctly renders seeded database data
  - Confirms the View layer successfully displays IDs, descriptions, and statuses

- test(e2e): complete update command suite
  - Verified successful task description updates and terminal output
  - Implemented failure cases for invalid IDs (non-numeric and missing)
  - Validated description constraints (empty strings and whitespace-only)

- test(e2e): implement delete command verification
  - Verified successful task removal and terminal confirmation
  - Confirmed 'ID not found' error handling for non-existent tasks
  - Validated numeric ID requirements via middleware checks in E2E flow
