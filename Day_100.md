# Building Projects

## GitHub User Activity

A simple CLI tool to fetch and display the recent activity of a GitHub user.

## Features

- Fetches recent public activity for any GitHub user.
- Simple, readable output in the terminal with color-coded event types.

## What i do today

- chore(project): initialize project planning and context
  - [x] GEMINI.md
  - [x] PLAN.md
- feat(init): setup project, dependencies, and base CLI
  - [x] Run `npm init -y` to generate `package.json`.
  - [x] Install dependencies: `node-fetch`, `commander`.
  - [x] Configure `package.json` with "type": "module" (for ES modules).
  - [x] Add "bin" entry to `package.json` for `github-activity` CLI.
  - [x] Create `index.js` file with basic setup and shebang.
  - [x] Verify functionality (run `node index.js --help`).
- feat(api): add npm start script and complete API fetcher feature
  - [x] Implement function to fetch events from `https://api.github.com/users/<username>/events`.
  - [x] Add basic error handling (e.g., handle 404 for user not found).
  - [x] Parse and display the response JSON in the terminal.
  - [x] Integrate fetching logic with `index.js`.
  - [x] Add `npm start` script to `package.json`.
  - [x] Verify functionality (run `npm start <username>`).
- docs: expand README and update documentation
  - [x] Update `PLAN.md` with current progress.
  - [x] Update `README.md` with project documentation.
- feat(format): implement basic event formatter and refactor index.js
  - [x] Implement `formatActivity(events)` function.
  - [x] Refactor `index.js` to separate fetching and formatting.
  - [x] Verify terminal output format against MVP.
- fix(format): fix event formatting and refactor index.js
  - [x] Refactor `index.js` to separate fetching and formatting.
  - [x] Verify terminal output format against MVP.
- feat(ui): add color-coding to terminal output using chalk
  - [x] Implement `formatActivity(events)` function.
  - [x] Verify terminal output format against MVP.

- Finish and push to GitHub
