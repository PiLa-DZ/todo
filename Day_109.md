# Building Projects

- fix: kill the race condition bug 🏎️💥
  - Replaced the shared test database with isolated per-worker files
  - Resolved "file busy" and "data corruption" errors during npx vitest
  - Massive speed boost: Tests now run in parallel without conflict

- test(e2e): verify mark command status transitions and validation
  - Validated successful transitions to 'done', 'in-progress', and 'not-done'
  - Verified that invalid status strings are caught by middleware and reported
  - Confirmed 'ID not found' error handling within the mark command context
  - Ensured terminal output correctly reflects the updated task state

- test(e2e): implement comprehensive list command verification
  - Verified full task list rendering with seeded data
  - Confirmed empty state handling and 'no tasks' reporting
  - Validated status filtering logic for 'done', 'in-progress', and 'not-done'
  - Ensured invalid filter arguments are handled gracefully via middleware

- This is a great way to end the project—fixing a UX "papercut."
  - refactor(ux): show help menu instead of error for empty arguments
  - Updated argument validation to allow empty inputs to reach the help controller
  - Improved CLI friendliness by guiding users rather than throwing system errors

- refactor: simplify boot flow by removing redundant argument validation
  - Deleted validate-argv-length.js middleware
  - Delegated all input handling to the router's existing default logic
  - Fixed UX issue where empty inputs caused a system error instead of showing help
  - Confirmed all E2E help-page tests now pass 100%

- test(integration): fix timezone dependency in timestamp verification
  - Implemented vi.useFakeTimers to stabilize time-dependent assertions
  - Switched to UTC ISO strings for date verification to ensure cross-region compatibility
  - Verified that updatedAt field is only modified on targeted records
  - Decoupled test logic from local machine timezone offsets

- feat(view): complete localized date implementation & test synchronization
  - Refactored printSuccessOperition and viewListTasks to include human-readable timestamps
  - Updated all E2E test suites to validate new terminal alignment and date displays
  - Standardized ISO 8601 storage and en-GB localized viewing
  - Maintained clean version history within view files for future reference

- refactor(repo): implement Repository Pattern for centralized data access
  - Created taskRepository to encapsulate read/write operations
  - Decoupled services from DATA_FOLDER and DB_FILE constants
  - Simplified service layer logic by standardizing data retrieval and persistence
  - Verified system integrity with a full pass of the E2E and Integration test suites
